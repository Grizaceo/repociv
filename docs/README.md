# RepoCiv — Documentación

Esta carpeta ya no es el lugar de verdad para el trabajo vivo. La documentación activa del repo ahora se concentra en:

- `README.md` — visión general, arranque y estado actual del producto
- `deploy/systemd/README.md` — operación / servicios permanentes
- `execplan/repociv-harness-control-plane.md` — plan ejecutable vivo

La documentación histórica y los handoffs cerrados quedaron en `docs/archive/`.

Si llegaste aquí buscando el estado actual del proyecto: empieza por `README.md` y luego valida contra `src/main.ts`, `package.json` y los tests.
