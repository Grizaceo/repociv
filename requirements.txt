# ─── RepoCiv backend (Python) ─────────────────────────────────────────────────
# Mantener versiones m\u00ednimas; el lock real lo da el venv local.

# MCP server (stdio transport — expone bridge como tools para agentes externos)
mcp>=1.2

# Test runner
pytest>=9.0
pytest-asyncio>=0.24
pytest-cov>=5.0

# HTTP / async
httpx>=0.28
anyio>=4.0
aiohttp>=3.9
requests>=2.32

# WebSocket (Phase 1: bidirectional transport)
websockets>=12.0

# Schemas
pydantic>=2.0

# Persistencia / ledger anal\u00edtico (Fase 0)
# research_ledger.py degrada a "sin anal\u00edtica" si falta, pero el invariante
# DuckDB-as-materialized-view requiere que est\u00e9 instalado en producci\u00f3n.
duckdb>=1.0

# Hooks declarativos por repo (Fase 1, H5)
# repociv_hooks.py parsea repociv.yaml en cada repo gestionado.
PyYAML>=6.0

# Linter (CI gate; also used locally via scripts/check.sh)
ruff==0.15.5
